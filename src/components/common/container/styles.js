import {StyleSheet} from 'react-native';
import {moderateScale} from '../../../assets/theme/scaling';

// define your styles
export const styles = StyleSheet.create({
  wrapper: {
    padding: moderateScale(20),
  },
});
